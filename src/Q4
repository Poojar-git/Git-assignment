A developer creates and switches to a feature branch, makes multiple changes to the same file with separate commits, identifies the commit hash(es) of the specific commit(s) to copy, switches to the target branch, and uses git cherry-pick <commit-hash> to apply only the selected commit(s) without merging the entire branch.
