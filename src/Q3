A developer creates a feature branch, makes changes, commits and pushes them, then opens a Pull Request; if another developer modifies the same file on the main branch and pushes those changes, the Pull Request will have a merge conflict, which can be resolved by rebasing the feature branch onto the latest main, manually fixing any conflicts, continuing the rebase, and force-pushing the updated feature branch before merging the Pull Request.
